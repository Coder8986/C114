function preload() {
    
}

function setup(){
    canvas = createCanvas(600,800);
    canvas.center();
    video = createCapture(VIDEO);
    video.hide();
}

function draw() {
    
}

function save() {
    save(img.png);
}